<launch>  
  <arg name="gazebo" default="true" />

  <!-- sample parameter which are loaded in both app and sim-->
  <rosparam if="$(arg gazebo)" file="$(find io_gazebo_turtlebot_bringup)/config/common_config.yaml" command="load" ns="/" />
</launch>
